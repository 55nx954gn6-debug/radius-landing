# Radius Landing Page

Static GitHub Pages landing page for `www.radius-app.org`.

## DNS

- `www` CNAME -> `55nx954gn6-debug.github.io`
- bare `@` can redirect to `https://www.radius-app.org/` from Namecheap, or use GitHub Pages apex A records later.
